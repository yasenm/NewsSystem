@using NewsSystem.Data.ViewModels.Categories

@model CategoryCheckboxViewModel

@Html.HiddenFor(m => m.Id)
@Html.HiddenFor(m => m.Title)

<div class="checkbox">
    <label>
        @Html.CheckBoxFor(m => m.IsChecked)
        @Html.DisplayFor(m => m.Title)
    </label>
</div>